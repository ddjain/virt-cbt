apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cbt-backup-output
  namespace: cbt-demo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ocs-storagecluster-ceph-rbd
